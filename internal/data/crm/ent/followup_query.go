// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/go-keg/monorepo/internal/data/crm/ent/customer"
	"github.com/go-keg/monorepo/internal/data/crm/ent/followup"
	"github.com/go-keg/monorepo/internal/data/crm/ent/predicate"
)

// FollowUpQuery is the builder for querying FollowUp entities.
type FollowUpQuery struct {
	config
	ctx          *QueryContext
	order        []followup.OrderOption
	inters       []Interceptor
	predicates   []predicate.FollowUp
	withCustomer *CustomerQuery
	withFKs      bool
	loadTotal    []func(context.Context, []*FollowUp) error
	modifiers    []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FollowUpQuery builder.
func (fuq *FollowUpQuery) Where(ps ...predicate.FollowUp) *FollowUpQuery {
	fuq.predicates = append(fuq.predicates, ps...)
	return fuq
}

// Limit the number of records to be returned by this query.
func (fuq *FollowUpQuery) Limit(limit int) *FollowUpQuery {
	fuq.ctx.Limit = &limit
	return fuq
}

// Offset to start from.
func (fuq *FollowUpQuery) Offset(offset int) *FollowUpQuery {
	fuq.ctx.Offset = &offset
	return fuq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fuq *FollowUpQuery) Unique(unique bool) *FollowUpQuery {
	fuq.ctx.Unique = &unique
	return fuq
}

// Order specifies how the records should be ordered.
func (fuq *FollowUpQuery) Order(o ...followup.OrderOption) *FollowUpQuery {
	fuq.order = append(fuq.order, o...)
	return fuq
}

// QueryCustomer chains the current query on the "customer" edge.
func (fuq *FollowUpQuery) QueryCustomer() *CustomerQuery {
	query := (&CustomerClient{config: fuq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fuq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fuq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(followup.Table, followup.FieldID, selector),
			sqlgraph.To(customer.Table, customer.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, followup.CustomerTable, followup.CustomerColumn),
		)
		fromU = sqlgraph.SetNeighbors(fuq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first FollowUp entity from the query.
// Returns a *NotFoundError when no FollowUp was found.
func (fuq *FollowUpQuery) First(ctx context.Context) (*FollowUp, error) {
	nodes, err := fuq.Limit(1).All(setContextOp(ctx, fuq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{followup.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fuq *FollowUpQuery) FirstX(ctx context.Context) *FollowUp {
	node, err := fuq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FollowUp ID from the query.
// Returns a *NotFoundError when no FollowUp ID was found.
func (fuq *FollowUpQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fuq.Limit(1).IDs(setContextOp(ctx, fuq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{followup.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fuq *FollowUpQuery) FirstIDX(ctx context.Context) int {
	id, err := fuq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FollowUp entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FollowUp entity is found.
// Returns a *NotFoundError when no FollowUp entities are found.
func (fuq *FollowUpQuery) Only(ctx context.Context) (*FollowUp, error) {
	nodes, err := fuq.Limit(2).All(setContextOp(ctx, fuq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{followup.Label}
	default:
		return nil, &NotSingularError{followup.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fuq *FollowUpQuery) OnlyX(ctx context.Context) *FollowUp {
	node, err := fuq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FollowUp ID in the query.
// Returns a *NotSingularError when more than one FollowUp ID is found.
// Returns a *NotFoundError when no entities are found.
func (fuq *FollowUpQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fuq.Limit(2).IDs(setContextOp(ctx, fuq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{followup.Label}
	default:
		err = &NotSingularError{followup.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fuq *FollowUpQuery) OnlyIDX(ctx context.Context) int {
	id, err := fuq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FollowUps.
func (fuq *FollowUpQuery) All(ctx context.Context) ([]*FollowUp, error) {
	ctx = setContextOp(ctx, fuq.ctx, ent.OpQueryAll)
	if err := fuq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FollowUp, *FollowUpQuery]()
	return withInterceptors[[]*FollowUp](ctx, fuq, qr, fuq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fuq *FollowUpQuery) AllX(ctx context.Context) []*FollowUp {
	nodes, err := fuq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FollowUp IDs.
func (fuq *FollowUpQuery) IDs(ctx context.Context) (ids []int, err error) {
	if fuq.ctx.Unique == nil && fuq.path != nil {
		fuq.Unique(true)
	}
	ctx = setContextOp(ctx, fuq.ctx, ent.OpQueryIDs)
	if err = fuq.Select(followup.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fuq *FollowUpQuery) IDsX(ctx context.Context) []int {
	ids, err := fuq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fuq *FollowUpQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fuq.ctx, ent.OpQueryCount)
	if err := fuq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fuq, querierCount[*FollowUpQuery](), fuq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fuq *FollowUpQuery) CountX(ctx context.Context) int {
	count, err := fuq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fuq *FollowUpQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fuq.ctx, ent.OpQueryExist)
	switch _, err := fuq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fuq *FollowUpQuery) ExistX(ctx context.Context) bool {
	exist, err := fuq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FollowUpQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fuq *FollowUpQuery) Clone() *FollowUpQuery {
	if fuq == nil {
		return nil
	}
	return &FollowUpQuery{
		config:       fuq.config,
		ctx:          fuq.ctx.Clone(),
		order:        append([]followup.OrderOption{}, fuq.order...),
		inters:       append([]Interceptor{}, fuq.inters...),
		predicates:   append([]predicate.FollowUp{}, fuq.predicates...),
		withCustomer: fuq.withCustomer.Clone(),
		// clone intermediate query.
		sql:       fuq.sql.Clone(),
		path:      fuq.path,
		modifiers: append([]func(*sql.Selector){}, fuq.modifiers...),
	}
}

// WithCustomer tells the query-builder to eager-load the nodes that are connected to
// the "customer" edge. The optional arguments are used to configure the query builder of the edge.
func (fuq *FollowUpQuery) WithCustomer(opts ...func(*CustomerQuery)) *FollowUpQuery {
	query := (&CustomerClient{config: fuq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fuq.withCustomer = query
	return fuq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FollowUp.Query().
//		GroupBy(followup.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fuq *FollowUpQuery) GroupBy(field string, fields ...string) *FollowUpGroupBy {
	fuq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FollowUpGroupBy{build: fuq}
	grbuild.flds = &fuq.ctx.Fields
	grbuild.label = followup.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.FollowUp.Query().
//		Select(followup.FieldCreatedAt).
//		Scan(ctx, &v)
func (fuq *FollowUpQuery) Select(fields ...string) *FollowUpSelect {
	fuq.ctx.Fields = append(fuq.ctx.Fields, fields...)
	sbuild := &FollowUpSelect{FollowUpQuery: fuq}
	sbuild.label = followup.Label
	sbuild.flds, sbuild.scan = &fuq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FollowUpSelect configured with the given aggregations.
func (fuq *FollowUpQuery) Aggregate(fns ...AggregateFunc) *FollowUpSelect {
	return fuq.Select().Aggregate(fns...)
}

func (fuq *FollowUpQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fuq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fuq); err != nil {
				return err
			}
		}
	}
	for _, f := range fuq.ctx.Fields {
		if !followup.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fuq.path != nil {
		prev, err := fuq.path(ctx)
		if err != nil {
			return err
		}
		fuq.sql = prev
	}
	return nil
}

func (fuq *FollowUpQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FollowUp, error) {
	var (
		nodes       = []*FollowUp{}
		withFKs     = fuq.withFKs
		_spec       = fuq.querySpec()
		loadedTypes = [1]bool{
			fuq.withCustomer != nil,
		}
	)
	if fuq.withCustomer != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, followup.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FollowUp).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FollowUp{config: fuq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(fuq.modifiers) > 0 {
		_spec.Modifiers = fuq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fuq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := fuq.withCustomer; query != nil {
		if err := fuq.loadCustomer(ctx, query, nodes, nil,
			func(n *FollowUp, e *Customer) { n.Edges.Customer = e }); err != nil {
			return nil, err
		}
	}
	for i := range fuq.loadTotal {
		if err := fuq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (fuq *FollowUpQuery) loadCustomer(ctx context.Context, query *CustomerQuery, nodes []*FollowUp, init func(*FollowUp), assign func(*FollowUp, *Customer)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*FollowUp)
	for i := range nodes {
		if nodes[i].customer_follow_ups == nil {
			continue
		}
		fk := *nodes[i].customer_follow_ups
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(customer.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "customer_follow_ups" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (fuq *FollowUpQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fuq.querySpec()
	if len(fuq.modifiers) > 0 {
		_spec.Modifiers = fuq.modifiers
	}
	_spec.Node.Columns = fuq.ctx.Fields
	if len(fuq.ctx.Fields) > 0 {
		_spec.Unique = fuq.ctx.Unique != nil && *fuq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fuq.driver, _spec)
}

func (fuq *FollowUpQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(followup.Table, followup.Columns, sqlgraph.NewFieldSpec(followup.FieldID, field.TypeInt))
	_spec.From = fuq.sql
	if unique := fuq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fuq.path != nil {
		_spec.Unique = true
	}
	if fields := fuq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, followup.FieldID)
		for i := range fields {
			if fields[i] != followup.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fuq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fuq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fuq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fuq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fuq *FollowUpQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fuq.driver.Dialect())
	t1 := builder.Table(followup.Table)
	columns := fuq.ctx.Fields
	if len(columns) == 0 {
		columns = followup.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fuq.sql != nil {
		selector = fuq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fuq.ctx.Unique != nil && *fuq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range fuq.modifiers {
		m(selector)
	}
	for _, p := range fuq.predicates {
		p(selector)
	}
	for _, p := range fuq.order {
		p(selector)
	}
	if offset := fuq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fuq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (fuq *FollowUpQuery) Modify(modifiers ...func(s *sql.Selector)) *FollowUpSelect {
	fuq.modifiers = append(fuq.modifiers, modifiers...)
	return fuq.Select()
}

// FollowUpGroupBy is the group-by builder for FollowUp entities.
type FollowUpGroupBy struct {
	selector
	build *FollowUpQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fugb *FollowUpGroupBy) Aggregate(fns ...AggregateFunc) *FollowUpGroupBy {
	fugb.fns = append(fugb.fns, fns...)
	return fugb
}

// Scan applies the selector query and scans the result into the given value.
func (fugb *FollowUpGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fugb.build.ctx, ent.OpQueryGroupBy)
	if err := fugb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FollowUpQuery, *FollowUpGroupBy](ctx, fugb.build, fugb, fugb.build.inters, v)
}

func (fugb *FollowUpGroupBy) sqlScan(ctx context.Context, root *FollowUpQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fugb.fns))
	for _, fn := range fugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fugb.flds)+len(fugb.fns))
		for _, f := range *fugb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fugb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fugb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FollowUpSelect is the builder for selecting fields of FollowUp entities.
type FollowUpSelect struct {
	*FollowUpQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fus *FollowUpSelect) Aggregate(fns ...AggregateFunc) *FollowUpSelect {
	fus.fns = append(fus.fns, fns...)
	return fus
}

// Scan applies the selector query and scans the result into the given value.
func (fus *FollowUpSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fus.ctx, ent.OpQuerySelect)
	if err := fus.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FollowUpQuery, *FollowUpSelect](ctx, fus.FollowUpQuery, fus, fus.inters, v)
}

func (fus *FollowUpSelect) sqlScan(ctx context.Context, root *FollowUpQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fus.fns))
	for _, fn := range fus.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fus.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (fus *FollowUpSelect) Modify(modifiers ...func(s *sql.Selector)) *FollowUpSelect {
	fus.modifiers = append(fus.modifiers, modifiers...)
	return fus
}
